{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "like_button.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run babel & npm run tw",
    "babel": "npx babel --watch src --out-dir . --presets react-app/prod",
    "tw": "npx tailwindcss -i ./src/app.css -o ./app.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "tailwindcss": "^3.4.1"
  }
}
